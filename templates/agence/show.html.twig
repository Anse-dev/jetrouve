{% extends 'base.html.twig' %}

{% block title %}Agence{% endblock %}

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ agence.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ agence.roles ? agence.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ agence.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agence_index') }}">back to list</a>

    <a href="{{ path('app_agence_edit', {'id': agence.id}) }}">edit</a>

    {{ include('agence/_delete_form.html.twig') }}
{% endblock %}
